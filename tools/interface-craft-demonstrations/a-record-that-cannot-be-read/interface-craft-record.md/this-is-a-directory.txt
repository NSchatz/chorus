interface-craft-record.md beside this file is a DIRECTORY and not a file, which
is how a record that is there and cannot be read is committed to git. A mode of
000 would not survive a checkout; a directory does.

Absent and unreadable are different faults with different remedies, so they are
different exit codes and different trees.
