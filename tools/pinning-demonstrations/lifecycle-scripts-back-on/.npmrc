# WRONG ON PURPOSE. Lifecycle scripts are back on and nothing here says why, so
# a later reader cannot tell this from an accident. P4 allows the opt-back-in
# and requires the reason beside it; tools/pinning-check.sh requires this tree
# to go red naming P4.
#
# The comment above is deliberately NOT adjacent to the setting: a blank line
# sits between them, which is what a reason that drifted away from its setting
# looks like.

ignore-scripts = false
