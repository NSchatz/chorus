[package]
name = "chorus-sync"
description = "chorus clock synchronisation: virtual clocks, jitter models, servo, deterministic simulator"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
