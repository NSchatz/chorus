[package]
name = "chorus-measure"
description = "the measurement harness: capture ingest, inter-device lag, free-run drift, and the saved report"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
chorus-alsa = { path = "../alsa" }
chorus-audio = { path = "../audio" }

[dev-dependencies]
chorus-audio-path = { path = "../audio-path" }
