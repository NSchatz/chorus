# Canonical input for the ungroup golden vector.
#
# Encoding these field values must produce exactly ungroup.json, and decoding
# ungroup.json must recover exactly these field values.
#
# Format: one "key = value" per line. A line whose first non-space character is
# '#' is a comment; a '#' anywhere else is an ordinary character.

message_type = ungroup
zone = kitchen
