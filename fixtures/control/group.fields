# Canonical input for the group golden vector.
#
# Encoding these field values must produce exactly group.json, and decoding
# group.json must recover exactly these field values.
#
# Format: one "key = value" per line. A line whose first non-space character is
# '#' is a comment; a '#' anywhere else is an ordinary character.

message_type = group
zone = kitchen
group = downstairs
